# Deployment Test Configuration
# Disable Hibernate ORM for deployment tests
# The ParameterSetEntity is optional and only used when EXTERNAL mode is enabled
# Since deployment tests don't use external parameter storage, we disable ORM
quarkus.hibernate-orm.enabled=false
