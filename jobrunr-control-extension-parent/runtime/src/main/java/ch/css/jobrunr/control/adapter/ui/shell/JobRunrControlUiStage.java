package ch.css.jobrunr.control.adapter.ui.shell;

/**
 * Deployment stage shown in the shared UI shell.
 */
public record JobRunrControlUiStage(
        String label,
        String styleClass,
        String icon) {
}
