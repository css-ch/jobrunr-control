package ch.css.jobrunr.control.domain;

/**
 * Enum for supported job parameter types.
 * Defines the allowed data types for job parameters.
 */
public enum JobParameterType {
    STRING,
    INTEGER,
    BOOLEAN,
    DATE,
    DATETIME
}

